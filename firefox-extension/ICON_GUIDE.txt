ICON GENERATION GUIDE
════════════════════

The firefox-extension/icons/ folder needs three PNG files:
- icon-16.png   (16x16 pixels)
- icon-48.png   (48x48 pixels)  
- icon-128.png  (128x128 pixels)

QUICK GENERATION (using ImageMagick):
════════════════════════════════════════

1. Install ImageMagick:
   brew install imagemagick

2. Create a base SVG icon:
   Create an SVG file with the Decoy Service logo

3. Convert to PNG:
   convert -density 300 icon.svg -resize 16x16 icon-16.png
   convert -density 300 icon.svg -resize 48x48 icon-48.png
   convert -density 300 icon.svg -resize 128x128 icon-128.png

ONLINE TOOL:
════════════════════════════════════════

1. Go to: https://favicon-generator.org/
2. Upload your icon or design one
3. Download PNG files
4. Copy to firefox-extension/icons/

DESIGN IDEAS:
════════════════════════════════════════

Good icons for Decoy Service:
- 🔒 Lock icon (privacy)
- 🕵️ Detective/spy theme
- 🎭 Mask (obfuscation)
- 🌀 Spiral (confusion)
- 👁️ Eye with slash (anti-tracking)
- 🛡️ Shield (protection)

SIMPLE PYTHON SCRIPT:
════════════════════════════════════════

from PIL import Image, ImageDraw, ImageFont

def create_icon(size, filename):
    # Create a new image
    img = Image.new('RGB', (size, size), color='#2563eb')  # Blue background
    draw = ImageDraw.Draw(img)
    
    # Draw a simple shield/lock shape
    padding = size // 4
    draw.rectangle(
        [(padding, padding), (size-padding, size-padding)],
        outline='white',
        width=2
    )
    draw.text((size//2 - 4, size//2 - 8), '🔒', fill='white')
    
    img.save(filename)

# Generate icons
create_icon(16, 'icon-16.png')
create_icon(48, 'icon-48.png')
create_icon(128, 'icon-128.png')

TEMPORARY FIX:
════════════════════════════════════════

For testing, you can use placeholder base64 PNG:

Edit manifest.json and use data URIs:
{
  "icons": {
    "16": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAABAAAAAQCAYAAAAf8/9hAAAA...",
    ...
  }
}

════════════════════════════════════════
Once you have proper icons, place them
in firefox-extension/icons/ folder
════════════════════════════════════════
